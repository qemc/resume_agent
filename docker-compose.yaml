services:
  backend:
    build: ./apps/backend
    env_file:
      - ./apps/backend/.env

  frontend:
    build: ./apps/frontend
    ports:
      - "80:80"
    depends_on:
      - backend

# docker compose --env-file ./apps/backend/.env up -d
